spring.application.name=v1

#mysql config
spring.datasource.url=jdbc:mysql://localhost:3306/urlshortener?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=rootkK@123

# Explicitly set the dialect so Hibernate never needs to probe the DB on startup
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Disable open-session-in-view (best practice; avoids lazy-load surprises)
spring.jpa.open-in-view=false

#redis config
spring.data.redis.host=localhost
spring.data.redis.port=6379